
# IP 协议

IP 是 Internet Protocol(国际互连协议) 的缩写。
### IP 协议格式
#### MAC 帧

#### IP 数据段格式
![[../img/posts/uncategorized/IP 协议数据结构.png]]
- **版本 (4 bits)**: IP 版本号，IPv4 = 4, IPv6 = 6.
- **首部长度 (4 bits)**: 以 32bit (4 字节) 为单位，首部长度一般填 5, 即 20 字节的长度.
- **服务类型 (8 bits)**: 默认为 0 就行，用于一般服务.
- **总长度 (16 bits)**: 整个 IP 数据报的长度 (首部 + 数据段), 以字节为单位.
- **标识 (16 bits)**: 标识符, 用于分片重组
- **标志 (3 bits)**: 
	- bit2: 保留位
	- bit1: 填 1 表示禁止分片, 填 0 表示允许分片
	- bit0: 若为分片包的最后一个包就填0, 否则为 1
- **片偏移 (13 bits)**: 用于接收端重组报文
- **生存时间 (8 bits)**: IP 报文在网络传输中可以通过网络交换机的次数, 若超过这个次数则交换机会将本次的 IP 报丢弃。作用是防止 IP 数据报在网络中长时间传输导致网络瘫痪。常用值: 64/128
- **协议 (4 bits)**: 上层协议 (ICMP=1, TCP=6, UDP=17)
- **首部校验和**: 首部校验和，[首部校验和计算方法](#首部校验和计算方法)
- **源 IP 地址**: 发送方的 IP 地址
- **目的 IP 地址**: 接收方的 IP 地址
- **可选字段**: 可变, 0-40 字节的长度, 需填充至 32 位边界。但在现代网络通信中这段内容几乎用不到。
- **负载数据**：用户传输的负载数据
### 首部校验和计算方法
假设 IP 首部经过填充后如下图
![[../img/posts/uncategorized/IP 首部填充后数据.png]]
校验和计算（从 bit 0 开始）
```text
0x4500 + 0x003C +  (0x003C: IP 首部长度 20, 数据段 40)
0x0000 + 0x4000 + 
0x4001 + 0x0000(校验和部分计算时强制为0) + 
0xC0A8 + 0x0166(源 IP 地址部分) +
0xC0A8 + 0x010A(目的 IP 地址部分)
= 0x2_48FD (高 16 位有数据)

将高 16 位数据单独拿出来与低 16 位再次进行加法运算
0x0002 + 0x48FD = 0x48FF (高 16 位没有数据了)

最终计算结果按位取反得到 0xB700
```

# ICMP 协议

### ICMP 协议格式
#### MAC 帧
![[../img/posts/uncategorized/IP 协议 MAC 帧.png]]
#### 数据段格式
![[../img/posts/uncategorized/ICMP 数据段格式.png]]
- **类型 (8 bits)**: ICMP 报文类型, 主要分为**查询报文**和**差错报文**两大类 [报文类型表](#报文类型表)
- **代码 (8bits)**: ICMP 差错报文类型, 根据ICMP差错报文的类型，进一步分析错误的原因，代码值不同对应的错误也不同. 
- **校验和(16 bits)**: 用于检查数据报文是否有错误。
- **标识符 (16 bits)**: 用于对每一个发送的数据报进行标识
- **序列号 (16 bits)**: 对发送的每一个数据报文进行编号，比如：发送的第一个数据报序列号为1，第二个序列号为2
- **负载数据**: 要发送的 ICMP 数据部分
#### 报文类型表

|   类型 (Type)    | 代码 (Code) |           描述            | 大类       |
| :------------: | :-------: | :---------------------: | -------- |
|       0        |     0     |     回显应答 (ping 应答)      | **查询报文** |
|       8        |     0     |     回显请求 (ping 请求)      | **‘’**   |
|       9        |     0     |          路由器通告          | **‘’**   |
|       10       |     0     |          路由器请求          | **‘’**   |
|       13       |     0     |     时间戳请求 (**已弃用**)     | **‘’**   |
|       14       |     0     |     时间戳应答 (**已弃用**)     | **‘’**   |
|       15       |     0     |     信息请求 **(已弃用)**      | **‘’**   |
|       16       |     0     |     信息应答 **(已弃用)**      | **‘’**   |
|       17       |     0     |         地址掩码请求          | **‘’**   |
|       18       |     0     |         地址掩码应答          | **‘’**   |
|  3 (目的不可达报文)   |     0     |          网络不可达          | *差错报文*   |
|       ‘’       |     1     |          主机不可达          | *‘’*     |
|       ‘’       |     2     |          协议不可达          | *‘’*     |
|       ‘’       |     3     |          端口不可达          | *‘’*     |
|       ‘’       |     6     |        无法找到目标网络         | *‘’*     |
|       ‘’       |     7     |        无法找到目标主机         | *‘’*     |
|       ‘’       |     9     |        目标网络被强制禁止        | *‘’*     |
|       ‘’       |    10     |        目标主机被强制隔离        | *‘’*     |
|       ‘’       |    11     | 由于 TOS(特定服务类型) 导致的网络不可达 | *‘’*     |
|       ‘’       |    12     | 由于 TOS(特定服务类型) 导致的主机不可达 | *‘’*     |
|       ‘’       |    13     |     由于过滤导致的通信被强制禁止      | *‘’*     |
|    4(源抑制报文)    |     0     |          源端被关闭          | *‘’*     |
| 5(ICMP 重定向报文)  |     0     |         对网络重定向          | *‘’*     |
|       ‘’       |     1     |         对主机重定向          | *‘’*     |
|       ‘’       |     2     |       对复位类型和网络重定向       | *‘’*     |
|       ‘’       |     3     |       对复位类型和主机重定向       | *‘’*     |
| 11(TTL 为 0 报文) |     0     |       传输期间生存时间为 0       | *‘’*     |
|       ‘’       |     1     |     在数据包组装期间生存时间为 0     | *‘’*     |
|   12(报文格式问题)   |     0     |        坏的 IP 首部         | *‘’*     |
|       ‘’       |     1     |         却是必须的选项         | *‘’*     |
# UDP 协议
### UDP 协议格式
#### MAC 帧
![[../img/posts/uncategorized/UDP 协议 MAC 帧.png]]
#### 数据段格式
![[../img/posts/uncategorized/UDP 数据段格式.png]]
- **IP 首部**: [IP 数据段格式](#IP%20数据段格式)
- **源端口号 (16 bits)**: 发送端的 16 位端口号
- **目的端口号 (16 bits)**: 接收端的 16 位端口号
- **UDP 长度 (16 bits)**: 16 位 UDP 数据长度, 包含 UDP 首部 + 负载数据部分, 单位: 字节
- **UDP 校验和 (16 bits)**: 包含三个部分：UDP 伪首部，UDP 首部和 UDP 的数据部分。伪首部的数据是从 IP 数据报头和 UDP 数据报头获取的，包括源IP地址，目的IP地址，协议类型和 UDP 长度，其目的是让UDP两次检查数据是否已经正确到达目的地。
